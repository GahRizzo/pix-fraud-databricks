[project]
name = "pix-fraud-databricks"
version = "1.0.1"
requires-python = ">=3.10"
dependencies = [
    "pyyaml>=6.0"
]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.setuptools.packages.find]
where = ["src"]