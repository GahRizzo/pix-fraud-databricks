bundle:
  name: pix-fraud-databricks

include:
  - databricks/resources/*.yml

artifacts:
  default:
    type: whl
    build: python -m build --wheel

targets:
  dev:
    mode: development
    default: true