bundle:
  name: pix-fraud-databricks

include:
  - databricks/resources/*.yml

artifacts:
  default:
    type: whl
    build: python -m pip wheel . -w dist --no-deps

targets:
  dev:
    mode: development
    default: true